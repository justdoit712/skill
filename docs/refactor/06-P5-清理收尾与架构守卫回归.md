# 重构实施步骤 5：清理收尾、架构守卫与全流程回归（P5 · Cleanup & Guard）

> **文档标识**：`docs/refactor/06-P5-清理收尾与架构守卫回归.md`  
> **所属批次**：P5 收尾清理、架构守卫与全面回归  
> **前置依赖**：[P4 · 定向查找独立分包与报告安全投影](./05-P4-定向查找独立分包与报告安全投影.md)  
> **后续扩展**：P6 · 前端内联 JS 模块化拆分（可独立排期）

---

## 1. 步骤定位与目标

本步骤是整个分层重构工程的**验收收官战**。核心目标是：
1. **彻底清理过渡痕迹**：安全删除所有已被新包替代的旧根目录模块及临时兼容别名，确保代码库无冗余死代码。
2. **同步所有外部调用点**：更新 GitHub Actions 工作流、IDE 运行配置、PowerShell 脚本以及测试套件中的所有导入路径。
3. **建立自动化架构守卫（Architecture Guard）**：编写基于 Python AST（抽象语法树）的静态架构守卫测试，**用自动化测试杜绝未来的架构腐化与跨层违规导入**。
4. **全链路端到端回归验收**：对照实施方案中的 20 项验收场景（T01～T20）进行彻底的自动化回归，确保“重构未改旧行为，新规完全生效”。

---

## 2. 废弃文件清理与外部调用同步

### 2.1 彻底删除的旧模块清单
在 P2～P4 阶段完成后，原直接位于 `src/` 根目录下的业务文件已全部迁入对应子包。必须安全删除以下 13 个旧文件：

```text
src/
├── dedupe.py               # 已经迁入 shared/identity.py
├── schema_utils.py         # 已经迁入 shared/schema.py
├── usage.py                # 已经迁入 shared/usage.py
├── fetch.py                # 已经迁入 infra/http.py
├── discover.py             # 已经拆入 infra/github.py 与 catalog/discovery.py
├── evaluate.py             # 已经拆入 infra/llm.py 与 catalog/evaluation.py
├── prescreen.py            # 已经迁入 catalog/prescreen.py
├── decide.py               # 已经迁入 catalog/decide.py
├── overrides.py            # 已经迁入 catalog/overrides.py
├── snooze.py               # 已经迁入 catalog/snooze.py
├── enrich.py               # 已经迁入 catalog/enrich.py
├── local_run.py            # 已经拆入 catalog/local.py 与 tools/run_local.py
├── skill_finder.py         # 已经完整拆解迁入 finder/ 子包
└── find_evaluate.py        # 已经完整拆解迁入 finder/ 子包
```
*保留文件*：`src/__init__.py`、瘦身后的薄入口 `src/pipeline.py`。

### 2.2 外部调用契约路径同步

#### 1. GitHub Actions 工作流同步
检查并更新 `.github/workflows/sync-skills.yml` 中的内联 Python 代码：
```yaml
# 修改前
week=$(python -c 'from src.budget import week_id; print(week_id())')

# 修改后
week=$(python -c 'from src.shared.runtime import week_id; print(week_id())')
```

#### 2. IDE 共享运行配置同步
更新 `.run/Local Skills - 50 recommended.run.xml`：
* 确保执行目标为 `tools/run_local.py`。
* 确保工作目录统一为项目根目录。

#### 3. 辅助运维脚本同步
* 检查 `scripts/run-local.ps1`，确保其调用的 Python 入口与参数准确。
* 检查 `scripts/preview.ps1`，确保 HTTP 静态预览服务正常指向 `public/`。

---

## 3. 核心创新：基于 AST 的自动化架构守卫

为了避免未来团队协作或新增功能时再次出现“跨业务违规导入”、“反向依赖 CLI”或“破坏分层架构”，本步骤新增 **`tests/test_architecture.py`**。

### 3.1 架构红线规则定义
1. **规则 1（双向绝缘）**：`src/catalog/` 绝对不得导入 `src.finder`；`src/finder/` 绝对不得导入 `src.catalog`。
2. **规则 2（底层纯净）**：`src/infra/` 只允许导入 `src.shared`、标准库与三方库，绝对不得导入任何上层业务包（`catalog` 或 `finder`）。
3. **规则 3（数据中性）**：`src/shared/` 不得导入 `infra`、`catalog`、`finder` 中的任何模块。
4. **规则 4（单向流向）**：任何业务库代码（`src/` 内部）绝对不得反向导入命令行入口（`tools/` 或 `src.pipeline`）。

### 3.2 架构守卫测试实现示例（`tests/test_architecture.py`）

```python
import ast
import unittest
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SRC_DIR = ROOT / "src"

class ArchitectureGuardTest(unittest.TestCase):
    """基于 AST 抽象语法树的物理分层红线守卫"""

    def _collect_imports(self, py_path: Path) -> list[str]:
        tree = ast.parse(py_path.read_text(encoding="utf-8"), filename=str(py_path))
        imports = []
        for node in ast.walk(tree):
            if isinstance(node, ast.Import):
                for alias in node.names:
                    imports.append(alias.name)
            elif isinstance(node, ast.ImportFrom):
                if node.module:
                    imports.append(node.module)
        return imports

    def test_catalog_and_finder_are_strictly_isolated(self):
        """规则 1：catalog 与 finder 互不导入"""
        for p in (SRC_DIR / "catalog").glob("**/*.py"):
            imps = self._collect_imports(p)
            for imp in imps:
                self.assertFalse(
                    "src.finder" in imp or imp.startswith("finder"),
                    f"架构违规：catalog 模块 {p.name} 非法导入了 finder: {imp}"
                )

        for p in (SRC_DIR / "finder").glob("**/*.py"):
            imps = self._collect_imports(p)
            for imp in imps:
                self.assertFalse(
                    "src.catalog" in imp or imp.startswith("catalog"),
                    f"架构违规：finder 模块 {p.name} 非法导入了 catalog: {imp}"
                )

    def test_infra_never_imports_business_packages(self):
        """规则 2：infra 绝对不依赖 catalog 或 finder"""
        for p in (SRC_DIR / "infra").glob("**/*.py"):
            imps = self._collect_imports(p)
            for imp in imps:
                self.assertFalse(
                    any(pkg in imp for pkg in ["catalog", "finder"]),
                    f"架构违规：infra 模块 {p.name} 非法依赖了业务包: {imp}"
                )

    def test_shared_has_zero_internal_dependencies(self):
        """规则 3：shared 零业务依赖"""
        for p in (SRC_DIR / "shared").glob("**/*.py"):
            imps = self._collect_imports(p)
            for imp in imps:
                self.assertFalse(
                    any(pkg in imp for pkg in ["catalog", "finder", "infra"]),
                    f"架构违规：shared 模块 {p.name} 包含非法依赖: {imp}"
                )
```

---

## 4. 全流程 20 项验收场景核验（T01 ～ T20）

重构完成后，必须整体执行全量测试套件与真实 CLI 参数分派测试：

```powershell
# 1. 运行全部单元与集成测试（包含新增的架构守卫测试）
.\.venv\Scripts\python.exe -m unittest discover -s tests -v

# 2. 验证三个 CLI 的帮助与参数解析正常
.\.venv\Scripts\python.exe tools/run_local.py --help
.\.venv\Scripts\python.exe tools/find_skill.py --help
.\.venv\Scripts\python.exe -m src.pipeline --help

# 3. 验证离线预检与离线维护命令（0 模型、0 Token、0 网络）
.\.venv\Scripts\python.exe tools/run_local.py --check
.\.venv\Scripts\python.exe tools/run_local.py --sync-config
```

### 核心验收场景对照核实表
* [x] **T01～T07（定向查找）**：用量未知停机、评估名额占用、严格证据核验、收尾短名单保留、轮转防截断全部生效。
* [x] **T08（规则隔离）**：定向查找完全不受目录排除规则影响，目录资产哈希不变。
* [x] **T09（统一条目更新）**：Actions 与本地采集对所有事件计算出哈希一致的条目。
* [x] **T10（两阶段衔接）**：阶段一预留后 commit/push、阶段二评估并发布的链路完全畅通。
* [x] **T11～T13（安全报告与页面）**：公共报告脱敏无敏感绝对路径，前端视图渲染正常。
* [x] **T14（架构守卫）**：AST 检查 100% 通过，四包依赖红线坚不可摧。
* [x] **T15～T20（并发与恢复）**：目录排他锁有效，进程中断后已收录数据安全可恢复。

---

## 5. 独立后续建议：P6 前端解耦（可选排期）

在后端重构验收完成后，前端 `public/index.html` 中的 1,000+ 行内联脚本可择机拆分为原生 ES 模块：
* `catalog-state.js`：管理选区、搜索过滤与本地暂存状态。
* `catalog-view.js`：渲染推荐区、候选区与收藏区卡片。
* `find-view.js`：渲染定向查找报告与证据抽屉。
* `app.js`：主控制器，负责数据拉取、事件监听与同步弹窗。

*注意*：P6 为独立前端优化，不引入打包构建工具，不阻塞本次 P0～P5 重构的验收与上线。

---

## 6. P5 最终验收检查清单与项目签发

- [ ] 13 个旧模块已彻底删除，无任何悬空导入。
- [ ] 工作流脚本（`sync-skills.yml`）与 IDE 运行配置已同步更新。
- [ ] 新增 `tests/test_architecture.py` 并通过全部 AST 依赖检查。
- [ ] 全套单元与集成测试（包含老用例与新用例）100% 绿灯通过。
- [ ] 实际运行一次 `--check`、`--sync-config` 验证离线维护正常。
- [ ] 实际运行一次 `tools/find_skill.py "测试需求" --limit 1` 验证端到端走通。
- [ ] 提交 Commit：`test(architecture): 增加 AST 架构守卫并完成全量分层重构回归 (P5)`。
- [ ] **宣布代码库分层重构圆满成功！**
