# Skills 导航目录二开需求

更新时间：2026-09-20  
状态：已合并需求、文件清理清单与实施设计；尚未开始程序改造或旧项目清理。  
本文为本次二开的统一依据，替代此前分开的需求文档与清理清单。已确认选择与待定设计分别标注，不把建议当作用户决定。2026-09-20 审查修订：补充生产部署归属、同步发布链路、领域来源、许可前置检查和可测试规则；设计要求不代表代码已经实现。

## 1. 已确认的产品定位

将整个仓库精简重建为公开的 Skills 导航项目。仅保留发现、筛选、分类、展示和每周更新需要的内容，不继续维护原有技能包合集、助手应用或其他附带产品。

用户通过静态网页了解技能的用途、查看推荐依据和更新记录，再点击原始链接自行访问上游。

- 不下载、镜像、安装、启用或执行完整技能包。
- 允许在运行过程中读取上游 README、SKILL.md 及必要的参考材料，用于识别、描述和评估。
- 持久化保存索引、中文简述、分类、评估依据、版本标识及更新记录，不保存完整技能包。
- 不收录 DSH 插件等无关内容。
- 旧项目中不服务于新目标的代码、数据、页面和文档应清理，不只是在旧项目旁增加一个新脚本。

具体文件处理见第 8 节“仓库清理与文件处理”。

## 2. 访谈已确认决定

| 项目 | 用户选择 |
| --- | --- |
| 改造方式 | 按新目标重建最小结构，清理无关旧内容 |
| 来源 | 官方仓库为基础，可信社区清单与 GitHub 广泛搜索补充 |
| 搜索规则 | 允许逐步维护和完善，不要求第一版一次解决所有筛选问题 |
| 展示分区 | 候选区＋推荐区，默认展示推荐区 |
| 人工参与 | 不要求用户逐条批准；证据充分的自动推荐，不确定的留候选区 |
| 评估方法 | 程序预筛＋AI 生成中文简述、分类和辅助评估 |
| 质量取舍 | 内容优先，不设 Star 数、发布时间的硬门槛 |
| 新技能 | 内容扎实、用途明确也可推荐，展示未经实测状态 |
| 领域范围 | 只收录用户指定领域，见第 3 节 |
| 网页 | 支持搜索和筛选的静态网页，不做管理后台或常驻后端 |
| 运行 | 每周通过 GitHub Actions 自动执行，也保留手动触发；本地另有 Python / PyCharm 入口 |
| 发布 | 用户仓库 `justdoit712/skill` 的 GitHub Pages；不使用上游域名 |
| 旧域名归属 | `skill.miyucaicai.cn` 属于上游，不修改其 DNS、Vercel 项目或部署 |
| 公开范围 | 代码、规则、目录、评估记录和更新报告公开；密钥仅放 Secrets |
| 模型服务 | 暂未选择，服务接口与模型名称可配置 |
| 每周评估总量 | Actions 新增与重评合计最多 50 个，优先复核发生变化的已有推荐，其余额度处理新增 |
| 首次初始化 | Actions 同样最多评估 50 个，优先官方来源并覆盖关注领域；本地目标见下方补充 |
| 既有条目 | 检查变化，未变化不重复调用 AI |
| 推荐条目待复核 | 内容变化后仍保留推荐，醒目标记待复核及原评估版本；复核不通过再降级 |
| 平台范围 | 不限平台收录，标注上游明确声明的特殊工具、运行环境和依赖；不猜测兼容性 |

**2026-09-21 本地运行需求补充（优先于本文其余部分对手动额度的泛指）**：
用户要求在本机、最好在 PyCharm 手动启动，一次寻找 **50 个新增推荐技能**，明确允许评估超过
50 个候选，单次输入＋输出最多 **100M（100,000,000）Token**。按现有规则筛选，不为凑数降低门槛。
达到目标、Token 停止额度或候选用完即结束；每次模型响应后累计用量，最后一次请求允许超过停止线。
接口 usage 缺失时停止后续付费调用，不把未知消耗记成零；控制台与本地报告展示输入、输出、推理及总 Token。
使用独立本地调用账本，已完成的相同评估复用；不更改 Actions 的周额度或自动发布。
下文“首次／每周最多评估 50 个”“定时与手动共享周额度”均指 Actions 及原周同步入口。
具体启动、配置和报告路径见 [运行说明](运行说明.md)。

## 3. 收录领域及边界

### 3.1 用途分类

| 主分类 | 收录内容 |
| --- | --- |
| 编程开发 | 前后端、重构、调试、测试、代码审查 |
| 文档办公 | Word、PDF、Excel、PPT 等 |
| 内容创作 | 文章、图片、视频、音频 |
| 产品与设计 | 需求分析、界面设计、原型 |
| 自动化 | 浏览器操作、文件处理、工具集成、工作流 |
| 云服务与运维 | 部署、CI/CD、基础设施 |
| 安全 | 漏洞检测、代码安全、审计 |
| 金融与投资 | 宏观、股票、期货、大宗商品及研究和策略辅助 |
| 心理健康 | 情绪记录、压力管理、心理健康信息理解 |
| 身体健康 | 睡眠、运动、饮食及身体健康信息理解 |

科研、通用数据分析、AI 开发不作为独立收录方向；直接服务上述用途的技能仍可纳入。例如股票数据分析归入金融，不因包含数据处理就排除。

支持一个主分类和多个用途标签。来源可信度、用途分类、适用平台分别记录。

### 3.2 金融与投资

不能仅按交易市场限定信息范围，需关注国内外事件及跨市场关联：

- 公司、行业、财报、公告和股票行情研究。
- 国内外宏观经济、货币与财政政策。
- 地缘政治、军事动向及经济影响，例如中东局势与石油供给。
- 期货、大宗商品、能源、黄金和白银。
- 利率、汇率及其与股票和商品市场的关联。
- 新闻采集、事实核验、事件跟踪和风险提醒。
- 选股、技术分析、回测、模拟交易、仓位和风险分析。

排除实盘自动下单类技能。地缘政治与军事信息在这里用于投资研究，不扩展为无关的军事用途目录。用户尚未给出实际交易市场清单，不凭空假定只覆盖 A 股或必须交易某个市场。

### 3.3 心理健康与身体健康

覆盖日常管理和健康信息理解：

- 情绪、压力、睡眠、运动、饮食、习惯养成。
- 医学资料查询、体检指标解释、症状信息整理和就医准备。

不收录面向专业临床诊断、治疗决策的技能。金融与健康类评估规则需要专门设计，不能把盈利或疗效宣传当成质量证据，也不能把被收录解释为专业认证。

## 4. 来源与条目识别

### 4.1 来源层级

1. 核实归属的团队官方仓库作为基础。
2. 可信社区清单提供发现线索，追溯到原作者仓库。
3. GitHub 搜索主动发现符合领域的新技能，搜索规则与排除规则可配置、可逐步改进。

Anthropic、Vercel、Cloudflare、Trail of Bits、Hugging Face、Google Labs、Stripe、Supabase、Expo、Sentry、Better Auth 等是初始官方来源候选，实施时核验具体仓库与路径。

GitHub 是托管平台，不是所有技能的发布方。官方、社区、聚合清单必须区分，旧 README 中的历史数量不作为当前事实。

官方开发团队列表不能代表全部领域来源。金融、心理健康、身体健康必须有独立的社区发现入口，不因缺少官方团队技能而放弃搜索或用开发技能充数。

### 4.2 识别粒度

- 优先识别有真实 SKILL.md 和任务说明的独立技能。
- 合集仓库尽量展开到具体技能，保留直达目录或说明文件的链接。
- 普通仓库、空模板、插件容器或整个合集不能直接计为一个独立技能。
- 多个来源指向同一技能时去重，保留全部发现依据。
- 不把上游 README 内出现的任意 GitHub 链接自动当作技能。
- DSH 插件无论由哪条渠道发现均排除。
- 不以某个 Agent 平台作为收录门槛。纯说明型技能和带辅助脚本的技能均可收录，只要满足用途和筛选要求；特殊工具、运行环境和依赖须明确标注。SKILL.md 能被读取不代表其依赖的工具在所有环境都可用。
- 不直接继承旧聚合的“可信”或“推荐”状态；旧数据最多作为重新核实的线索。

### 4.3 初始来源覆盖与发现计划

以下为采集种子及查询计划，不是推荐名单。社区条目只确认找到了相关仓库或说明入口，未实测、未完成许可及全部内容审查，不授予官方或可信标签。实施时保存核验时间、具体技能路径、来源版本和许可状态。

| 领域 | 初始入口或发现方向 | 必须执行的约束 |
| --- | --- | --- |
| 编程开发、产品与设计 | Anthropic、Vercel 等已列候选官方技能仓库 | 展开到具体技能，按实际用途分类 |
| 文档办公、内容创作 | Anthropic 文档及创作技能；社区写作、图片、视频技能 | 不能把官方整库都归入同一类别 |
| 自动化 | 浏览器、文件与工作流技能；官方来源＋社区清单 | 排除只有 MCP 服务、无独立技能说明的普通工具 |
| 云服务与运维 | Cloudflare 等候选官方仓库 | 核验云环境和工具依赖 |
| 安全 | Trail of Bits 等候选官方仓库 | 区分审计用途与其他能力，检查脚本和权限说明 |
| 金融与投资 | 下表的宏观、地缘、大宗商品与期货入口 | 逐技能筛选，拒绝实盘下单，不将调用官方数据接口误称为官方技能 |
| 心理健康 | 下表的心理教育入口；补充情绪记录、压力管理查询 | 医疗声明和隐私要求单独评估，不直接推荐 |
| 身体健康 | 下表的睡眠、运动、饮食入口 | 排除临床决策、处方或疗效承诺；补查资料依据 |

本轮联网查到的缺口领域线索（2026-09-20）：

| 线索链接 | 相关范围 | 初始状态与注意点 |
| --- | --- | --- |
| [gauss314/skills](https://github.com/gauss314/skills) | FRED 宏观、市场数据、期货与商品等 | 社区合集，需展开；同库含交易执行技能，不能全库收录 |
| [market-news-analyst](https://github.com/tradermonty/claude-trading-skills/blob/main/skills/market-news-analyst/SKILL.md) | 政策、地缘事件、能源与贵金属关联 | 社区候选，需核验其引用及信息时效规则 |
| [futures-research](https://github.com/thousandcents/futures-research/blob/main/SKILL.md) | 期货投研、现货与基差等 | 社区候选，需检查实际功能边界与依赖 |
| [mental-health-psychoeducation](https://github.com/JK-0001/skills/blob/main/mental-health-psychoeducation/SKILL.md) | 心理健康教育与应对信息 | 社区候选；含药物和自评相关内容，须核验是否超出本项目边界 |
| [SkillMedev/skills](https://github.com/SkillMedev/skills) | 可发现睡眠、运动和饮食相关技能 | 社区合集，需展开和核验依据；不能以目录描述替代技能评估 |

每个领域配置中英文搜索词，搜索至少约束到 `SKILL.md` 文件，例如宏观／macro、地缘／geopolitical、大宗商品／commodities、期货／futures、黄金／gold、白银／silver、心理教育／psychoeducation、压力／stress、睡眠／sleep、运动／exercise、营养／nutrition。词表是发现手段，命中后仍需判别实际用途；实现时适配 GitHub 搜索 API 的认证、语法、分页和限流。

`config/sources.json` 为每条来源保存 `id`、`url`、`source_type`、`domain_ids`、`discovery_method`、`verification_status`；`config/searches.json` 保存按领域的查询与排除条件。每周输出按领域的发现、预筛、评估、推荐和缺口数量。没有合格条目的领域显示缺口，不伪造覆盖；采集模块开发不以每类已经存在推荐技能为前提。

## 5. 候选区与推荐区

### 5.1 流程

发现 → 去重 → 领域与类型预筛 → 待评估队列 → AI 简述、分类与评估 → 候选或推荐。

不符合领域、非技能、DSH 插件等应在调用 AI 前尽可能排除。超过每周额度的条目排队，不能仅因未轮到评估就判为低质量。

### 5.2 推荐原则

- 内容优先，Star、使用量、维护记录是参考信号，不设置统一硬门槛。
- 任务明确，步骤、依赖或示例足以支持对用途的理解。
- 来源可追溯，介绍和实际内容相符。
- 没有明显可疑行为或与收录范围冲突的功能。
- 官方技能也需识别用途、匹配领域，不无条件推荐。
- 信息不足、判断不确定或模型调用失败的条目不强行推荐。
- 展示推荐理由、主要限制、证据链接和未经实测状态，不用不透明分数替代说明。
- 已推荐技能的内容变化后，保留推荐状态并醒目标记“内容已变化，待复核”；展示原评估对应的版本及评估时间，不把旧结论视为对新版本的验证。复核通过后更新评估依据并清除标记，复核不通过则降级至候选区；确认不符合收录范围的条目移出活跃目录并保留状态变更记录。
- 等待额度或模型调用失败时保持待复核标记，不自动生成通过结论。

第一版采用下述可测试的门槛规则，不先引入未经校准的百分制“质量分”。规则属于本次修订的实施基线，可根据样本维护版本；AI 辅助评估不等于功能测试或安全审计。

每次评估输出 `scope_match`、`purpose_clarity`、`instruction_completeness`、`evidence_traceability`、`dependency_transparency`、`risk_review`，每项为 `pass/fail/unknown`，附对应链接、文件位置或简短依据，另含 `domain_checks`、`reason_codes`、`rules_version` 和 `source_fingerprint`。纯文本技能明确无外部依赖也可通过依赖检查。

- 推荐阈值：上述各项均为 `pass`，相关领域检查全部通过，输出结构有效且证据可定位。
- 候选：属于范围内但存在 `unknown`、说明不完整或尚需核验，注明原因；不得用模型自报置信度替代证据。
- 排除：明确不属于范围、非 Skill、DSH、实盘下单、临床决策、凭据外传等硬性拒绝项。普通说明质量不足不等于恶意。
- 网络、解析和模型错误属于处理失败，不当作质量判定；已有推荐按待复核规则处理。

金融专项检查：记录数据来源、数据时间与市场适用性；区分事实、推断和预测；不得承诺收益；涉及回测的技能应说明成本、数据偏差与样本外验证等限制；没有回测功能时该检查明确记为不适用；不可包含不可分割的实盘下单核心功能。

健康专项检查：实质性健康结论有可追溯依据，不承诺治愈，不替代专业诊断或处方；涉及紧急情形时有适当的寻求专业帮助边界；需要保存或传输健康数据时说明位置、用途、权限与删除方式。只有免责声明而缺少实质依据不算通过。范围不明确或证据不足进入候选。

启用自动推荐前，以固定版本的样本覆盖低 Star 完整技能、空模板、DSH、缺失依赖、夸大收益、自动下单、缺少健康依据、临床决策和正常范围内样例，验证规则及原因码。测试决策逻辑使用固定评估输出；真实模型另做有限校准，不声称模型判断具有确定性。

### 5.3 AI 使用

- 模型服务、模型名称与调用配置可替换，暂不绑定供应商。
- 对新增或内容有变化的技能调用 AI；相同输入与相同规则版本复用结果。
- 每周新增与重评合计最多 50 个；优先复核发生变化的已有推荐，其余额度处理新增。首次同样最多 50 个，优先官方并兼顾领域覆盖。
- 超额条目进入后续队列；技能数上限不等于固定调用次数或费用上限，单条输入长度、调用次数与重试也需有界。
- 记录评估采用的来源版本、模型及规则版本，便于复核。
- 无模型配置或调用失败时不伪造中文简介和评估；保留待处理状态。
- 上游文本是不可信资料，不能作为更改筛选规则、读取密钥或执行命令的指令。

### 5.4 评估吞吐与覆盖预期

50 是每周新增与重评合计的上限，不是推荐产出保证。已有索引的 182 条与本地元数据所称 66 条有重复、过期及范围差异，不能直接作为 248 个有效候选。

| 情景（估算假设） | 处理 248 个不同候选所需周数 |
| --- | --- |
| 每周全部 50 个用于新增，无失败或重评 | 至少 5 周 |
| 每周 10 个重评，40 个用于新增 | 至少 7 周 |
| 每周 25 个重评，25 个用于新增 | 至少 10 周 |

52 周最多 2600 个评估名额，包含重评和失败占用，不等于 2600 个不同技能或推荐项。若重评长期占满额度，新增会停滞，报告必须显示积压和最老等待时间，不擅自突破用户选择的上限。

首轮在各领域有合格候选的前提下按领域轮转，争取每类最多约 5 个评估机会，同领域优先官方。定义“初步可用覆盖”为十类各有至少 2 个推荐技能，仅作为目标：若通过率为 40% 且分布均衡，50 个评估约产生 20 个推荐，理论上首周可能达到；实际可能多周或部分领域持续为空。每周按实测通过率与积压更新估算，不为了填满类别降低规则。

## 6. 索引与展示

每个已评估条目至少包含：

- 技能名称、原始链接、作者或团队。
- 一至两句中文简述，说明做什么及典型使用场景。
- 主分类、补充标签及上游声明的适用平台、特殊工具、运行环境和依赖；上游未说明的不得推测为全平台兼容。
- 来源类型、发现渠道及来源依据。
- 候选／推荐状态、独立的待复核标记、评估理由和限制；内容变化待复核时清楚区分上游当前版本与原评估版本。
- 首次发现、最近检查、内容变更时间或可获得的版本标识。
- 上游移除、暂时无法访问等状态。

不编造用途、平台兼容性和变更时间。最近检查时间不能冒充技能更新时间。

静态网页默认展示推荐区，支持候选区切换、关键词搜索、用途和来源筛选、条目详情与原始链接、最近更新及历史报告。不建设登录系统、运行技能功能或管理后台。

## 7. 每周运行和更新记录

- 使用 GitHub Actions，每周一次，支持手动触发。实施默认每周日北京时间 10:00，UTC cron 为 `0 2 * * 0`，可配置；不能保留旧每日表达式。调度可能延迟，不承诺准点运行。
- GitHub Pages 展示生成的静态产物，无常驻服务。
- 新增与重评共用每周最多 50 个的总额度，首次也适用。手动触发不能绕过周额度，需记录本周已消耗量。
- 已收录条目检查内容变化，只重新评估变化的条目。
- 具体技能内容的变化与仓库其他文件提交要区分，不能仅凭仓库最近提交就报技能更新。
- 报告区分新增、内容变化、分类或链接修正、推荐状态变化、上游移除及采集失败。
- 网络失败时保留上次有效数据，不做批量删除或虚假下架。
- 公共索引和网页使用同一数据来源，避免重复维护。
- API 密钥、认证头和含凭据的请求信息不进入日志、仓库或网页。
- 同步和部署任务需明确串联，确保每周生成结果确实发布，不能仅假定自动提交必然触发另一个任务。

### 7.1 发布归属与域名边界（用户已确认）

- 用户拥有 `justdoit712/skill`，当前本地 `origin` 也指向该仓库。
- `skill.miyucaicai.cn` 属于上游作者；本轮查询其 CNAME 为 `cname.vercel-dns.com`。`vercel.json` 是现有 Vercel 部署配置，不能再称为“备用配置”；但本地文件和 DNS 并不能独立证明上游 Vercel 项目的全部设置。
- 新项目采用自己的 Pages 地址，默认计划为 `https://justdoit712.github.io/skill/`，实际以 Pages 环境返回地址为准；本轮未启用或验证该站点。
- 不修改上游 DNS、Vercel 项目、Custom Domain 或上游仓库；不是域名迁移，也不承诺为上游旧地址设置重定向。
- 在用户仓库核实没有需要沿用的 Vercel 自动部署后，才删除该仓库内的 `vercel.json` 与 `.vercelignore`。即使清理旧 HTML 和数据，不推送到上游仓库。保留切换前提交以便恢复。
- 网页相对资源、数据请求、canonical、sitemap、robots 和 README 链接统一按用户 Pages 的 `/skill/` 子路径生成，不继续引用上游域名。无自定义域名需求，不生成指向上游域名的 `CNAME`。

### 7.2 同步、持久化、生成和部署的完整链路

现有同步只提交 `SKILL_SOURCES.json README.md`；现有部署仅稀疏检出 `public/` 后直接上传。两者不能沿用来承载新流程。

实施采用同一次顶层 workflow 中显式依赖的处理与部署步骤，不靠机器人提交触发第二次 `push`。GitHub 说明 `GITHUB_TOKEN` 触发的普通 push 不会启动新 workflow：[官方说明](https://docs.github.com/en/actions/how-tos/write-workflows/choose-when-workflows-run/trigger-a-workflow)。

1. **检出与预检**：同步入口完整检出用户仓库目标分支，包含 `src/`、`config/`、`data/` 和 `public/`。校验配置、密钥存在性、仓库写权限及数据结构。未准备好不调用付费模型。
2. **发现与预筛**：更新候选队列和上游指纹，不把待处理条目直接推荐；读取当前周的持久额度。
3. **预留额度**：在模型调用前，把本批名额预留写入 `data/state/` 并成功 commit/push；失败则停止付费调用。详情见 7.3。
4. **评估和生成**：处理本批条目，更新唯一索引 `data/catalog.json`、队列 `data/queue.json`、额度及运行记录 `data/state/`、更新报告 `data/reports/`。由这些数据在本 job 生成 `public/data/`、报告页面与构建清单。模型失败也要记录，不抹除上次有效条目。
5. **验证与提交**：验证索引、链接、分类、额度和页面结构；明确提交范围为 `data/catalog.json`、`data/queue.json`、`data/state/`、`data/reports/` 及生成的 `public/` 内容。自动任务不改代码、规则、README 或密钥；提交前审核状态和差异，禁止无差别 `git add .`。代码和配置由开发提交。
6. **发布同一产物**：数据与页面提交成功后，从同一工作区上传已经生成的 `public/` Pages artifact，携带此次数据提交 SHA 和索引摘要。部署 job 通过 `needs` 或可复用 workflow 显式接收该产物；不重新稀疏检出旧 `public/`，不在缺少数据和生成器的环境构建。
7. **部署验证**：记录部署结果与 artifact 对应 SHA，验证 `/skill/` 下的数据和页面。提交成功而部署失败时可仅重试部署该产物，不再次评估技能；产物过期则完整检出指定 SHA 后仅生成页面再发布，不调用模型。

`.github/workflows/sync-skills.yml` 作为顶层入口；`.github/workflows/deploy-pages.yml` 可改为被显式调用的 `workflow_call` 发布流程。人工代码 push 或仅发布请求走同一顶层入口的“仅构建／发布”分支，读取持久索引，不消耗 AI 额度。避免两个独立部署入口发布不同版本。

写数据与部署的顶层运行共用固定 concurrency group，`cancel-in-progress: false`；被调用的部署 workflow 不再申请同一锁，以免相互阻塞。额度仍以仓库状态为准，不能把 Actions 排队当作账本。遇到用户同时 push，使用正常非强制提交；push 冲突时停止后续付费调用并重新加载状态，不能覆盖他人的提交。

仅写数据步骤使用 `contents: write`，部署步骤使用 `pages: write` 与 `id-token: write`，其余最小读取权限。Fork 投稿及 PR 测试不获取模型密钥、不执行上游技能，正式带密钥流程只运行用户可信默认分支的代码。

### 7.3 周额度与失败恢复

- 以 `Asia/Shanghai` 的周一 00:00 起算一周，使用带年份的 ISO 周标识，例如 `2026-W38`；定时、手动和重跑共享账本，不提供“绕过额度”输入。
- `data/state/budget.json` 记录周标识、上限 50、已预留名额及对应评估 ID。评估 ID 包含技能稳定 ID、内容指纹、规则版本与模型配置版本；不同目标版本的重评也消耗名额。
- `data/state/evaluations/` 记录预留、调用尝试、完成／失败状态及输出摘要；记录无密钥、无个人健康或交易账户信息。
- 先持久化本批预留再调用模型，批处理异常或 runner 被终止也不释放已预留名额，以保守方式防止再次获得 50 个。已完成 ID 复用结果，未知是否成功的调用标为待恢复，不静默免费重跑。
- 设计默认每个名额最多 2 次调用尝试，首次与重试都在调用前持久化尝试计数；失败／超时后仍占本周名额。手动重跑不重置尝试计数。失败任务可下周重新排队并占新周名额。
- 网络请求、来源检查不占 AI 名额。`dry_run` 只验证配置、读取和计算计划，不调用模型、不写账本、不提交、不部署。若要付费试跑，必须走正式额度预留流程。
- 验证包括：新 runner 读回账本、同周两次手动运行、并发触发、预留后崩溃、模型超时、push 失败、周边界和跨年周标识；总预留不得超过 50。

### 7.4 配置、密钥及依赖边界

| 文件／位置 | 保存内容 |
| --- | --- |
| `config/sources.json`、`searches.json`、`taxonomy.json`、`rules.json`、`overrides.json` | 无密钥的来源、查询、分类、规则和人工覆盖，全部可提交 |
| `config/model.example.json` | 不含凭据的接口、模型名、超时、token 上限和重试配置示例 |
| GitHub Secrets／运行时环境变量 | `LLM_API_KEY` 等模型凭据；GitHub 访问使用最小权限的运行时 token |
| `.env.example` | 仅变量名和占位符，可提交 |
| `.env`、`.env.*`（`.env.example` 除外）、`config/*.local.json` | 本地私密配置，由实际 `.gitignore` 规则排除，不只是注释 |

`config.py` 当前只有示例 API Key，不代表已发现真实密钥泄漏，但不能继续把凭据与可提交配置混在同一文件。移除旧配置前检查调用方，改为显式读取环境变量；忽略规则无法撤回已提交秘密，如发现真实凭据先撤销／轮换，不自动重写历史。

第一版运行依赖基线采用 `requests`（HTTP）和 `PyYAML`（安全解析元数据），JSON、时间与静态模板生成尽量使用标准库；移除旧 `schedule`、`flask`、`flask-cors`。旧 HTML 爬取被替代后移除 `beautifulsoup4` 和 `lxml`。开发测试依赖单独管理，最终版本通过干净环境安装与检查后锁定，不把建议版本当成已验证版本。

### 7.5 README、投稿模板与公开产物

- 新 README 由人工维护，不再承载自动生成的全量技能列表或 ClawHub 提交信息。每周结果写入 `data/reports/` 与网页，README 只链接这些入口；删除旧自动插入标记和写入 README 的同步代码。
- 旧 README 是部分区块自动生成，不是整篇生成；`Latest commit: ff6c118` 属于 ClawHub 信息区，不是 README 本身的版本。
- 投稿模板分类值与 `config/taxonomy.json` 中第 3.1 节的十个分类严格一致；去掉独立“数据分析”“电商／营销”“视频／音频”等旧分类，适用条目映射到新主分类和标签。
- 必填项：技能名称、具体 SKILL.md 或技能目录链接、原始作者／仓库、来源类型（官方声明待核实／社区）、用途、主分类、依据链接或文件位置、依赖说明。记录许可信息（未知也可明示）和披露金融、健康或交易执行能力的字段。
- 投稿只创建待检查记录，不能直接写入推荐区或当作官方身份凭证。模板文案改为“可能加入目录”，不使用“加入官方列表”。测试保证十个分类一致、必要字段存在。

## 8. 仓库清理与文件处理

保留新产品需要的最小组成：采集评估代码、来源与规则配置、索引与状态、静态网页、每周任务、测试和必要说明。

明确清理旧本地技能包、助手子项目、Agent 团队框架、安装配置、聊天演示、备份下载功能、旧抓取流程、过时文档和宣传入口。

文件级清单在 第 8 节“仓库清理与文件处理”。先确认可复用代码与必要许可证，再移除被替代内容；保留 Git 历史，不重写历史。仅处理目标仓库中的已核实路径，不删除工作区外资料或用户未跟踪的本地配置。

### 8.1 清理依据

用户已选择将整个项目按新目标重建最小结构：公开的 Skills 链接导航、中文简介、指定领域分类、候选／推荐区、每周采集评估和静态发布。不保留无关旧产品。

下列目录按产品职责确定处理方向。实施前检查实时 Git 状态和具体引用；整目录清理仅针对已核实的仓库内容，不包括后来出现的用户未跟踪资料。迁移可复用代码时保留适用许可证、版权与署名。Git 历史保留，不做历史清洗。

### 8.2 删除：与目标无关的旧产品

| 路径 | 现有作用 | 处理 |
| --- | --- | --- |
| `skills/` | 本地技能包、脚本和素材集合 | 从新项目移除；需要的上游链接先作为待重新核实的线索提取，不保留技能包镜像 |
| `projects/` | companion、assistant、xiaoyue-web 等助手应用 | 删除整个旧产品目录及关联导航 |
| `antinet-agentteams/` | Agent 团队运行框架、演示、构建脚本和文档素材 | 删除，不属于导航采集器 |
| `finance-skills/` | 金融 MCP 安装配置和安装报告 | 删除；“关注金融”不等于需要保留旧 MCP 安装系统 |
| `templates/` | 伴侣角色注入模板 | 删除 |
| 根目录 `SKILL.md` | 小跃虚拟伴侣技能入口 | 删除，不把导航项目包装成伴侣技能 |
| `scripts/xiaoyue-companion.sh`、`scripts/xiaoyue-chat.ps1`、`scripts/xiaoyue-chat.js`、`scripts/xiaoyi-chat.ps1` | 聊天和伴侣启动脚本 | 删除 |
| `images/gzh-qr-topgo.jpg`、`images/wechat-qr.jpg` | 原项目推广二维码 | 删除文件及展示引用；如需保留作者署名，使用文本署名 |
| 根目录 `package-lock.json` | xiaoyue-companion-simple 的空包锁文件 | 删除；若新前端需要依赖，重新生成对应锁文件 |

### 8.3 删除：无关同步与旧展示

| 路径 | 处理 |
| --- | --- |
| `.github/workflows/sync_clawhub.yml` | 删除每天同步 ClawHub 软件包／CLI 的任务 |
| `scripts/sync_clawhub.py` | 删除对应脚本；其功能并非逐项发现真实技能 |
| `scripts/sync_skills.py` 中 DSH 配置 | 删除 DSH 来源、排除自身仓库配置及生成模板中的 DSH 分组 |
| `SKILL_SOURCES.json` | 提取需要重新核实的非 DSH 线索后，删除旧混合索引，由新模型替代；不保留旧推荐状态 |
| README 中 DSH、ClawHub Packages／CLI、本地包备份和助手推广区块 | 随新 README 重写删除 |
| `public/chat-demo.html`、`public/projects.html`、`public/local-skills.html` | 删除聊天、附带项目与本地技能包展示页 |
| `public/index-en.html`、`public/skills-en.html`、`README_EN.md` | 清除旧英文内容；第一版按已确认的中文导航实现，未来双语能力另行扩展 |
| `public/llms.txt`、`public/llms-full.txt`、`scripts/gen_llms_full.py` | 删除依赖旧聚合与本地技能统计的文件和生成器；新产品暂不需要这一入口 |
| `vercel.json`、`.vercelignore` | 删除备用部署配置，部署目标统一为 GitHub Pages |

DSH 不仅从既有索引删除，还要从新采集规则中排除，防止经其他来源重新进入。当前 DSH 来源引用的真实技能如需保留，须通过允许来源重新发现并评估。

### 8.4 新流程接通后删除：旧根目录运行链路

| 路径 | 现有职责与处理 |
| --- | --- |
| `main.py`、`crawler.py`、`scheduler.py`、`data_manager.py`、`config.py` | 旧爬虫、守护调度、存储和配置；迁移有用逻辑后删除旧实现，统一新入口 |
| `api_client.py`、`api_server_example.py` | 旧 API 同步和示例服务；新产品无需常驻 API 服务，删除 |
| `run_once.bat`、`start_daemon.bat`、`setup_scheduled_task.ps1` | 旧 Windows 本地运行与计划任务入口，删除 |
| `tools/skill_validator.py`、`tools/skill_validator/`、`tools/README.md` | 原本面向本地技能包验证和 XML prompt；评估可复用解析逻辑后迁移必要部分，删除旧工具入口 |
| `data/skills.json`、`data/local_skills.json`、`data/last_update.txt` | 旧索引和时间文件，不直接沿用为新目录；保留必要候选线索后由新数据结构替代 |
| `public/data/skills.json`、`public/data/local_skills.json`、`public/data/last_update.txt` | 清理旧展示副本，由统一的新索引生成页面数据 |

新数据仍可使用 `data/` 与 `public/data/` 目录，但必须规定唯一维护来源，公开产物由程序生成，不维持两套独立数据。

### 8.5 删除或合并：旧文档

以下旧说明在新 README 和操作文档就绪后删除，有价值的说明先整合：

- 根目录：`API_INTEGRATION.md`、`DEMO-SUCCESS.md`、`DEPLOYMENT_CHECKLIST.md`、`DEPLOYMENT_GUIDE.md`、`DEPLOYMENT.md`。
- 根目录：`FINAL-SUMMARY.md`、`PROJECT_SUMMARY.md`、`QUICKSTART.md`、`README-FINAL.md`、`SUMMARY.md`、`USAGE-GUIDE.md`、`XIAOYI-README.md`。
- 根目录：`技能清单.md`、`金融接口技能汇总.md`，不继续维护与实际导航不同步的额外清单。
- `docs/`：`D盘tool目录整理完成报告.md`、`整理完成总结.md`、`技能管理数据库.md`、`技能清理与迁移指南.md`、`技能数量差异分析报告.md`。
- `docs/`：`best-practices.md`、`quickstart.md`、`specification.md`，可用内容整合进新操作说明、数据结构和规则说明后删除旧版。

保留本合并文档，不使用通配符删除全部 Markdown。

### 8.6 保留并改写

| 路径 | 新职责 |
| --- | --- |
| `README.md` | 新产品定位、范围、浏览地址、运行方式、来源及局限 |
| `CONTRIBUTING.md`、`.github/ISSUE_TEMPLATE/submit-skill.yml` | 技能提交、纠错、领域及证据要求，不自动信任投稿 |
| `.github/workflows/sync-skills.yml` | 每周和手动采集、预筛、AI 评估、生成索引与更新报告 |
| `.github/workflows/deploy-pages.yml` | 静态发布，与同步流程可靠串联；可合并或复用工作流，避免重复部署 |
| `scripts/sync_skills.py` | 可重写为新入口或在新模块就绪后移除；不保留旧的全文链接即收录方式 |
| `public/index.html`、`public/styles.css` | 改造为中文导航、搜索、分类、候选／推荐、更新记录 |
| `public/skills.html` | 合并到主导航；合并完成后删除重复入口 |
| `public/robots.txt`、`public/sitemap.xml` | 如继续使用，按实际新页面和部署地址生成，移除旧地址 |
| `requirements.txt` | 只保留新程序依赖，移除旧助手、常驻服务和本地调度专用依赖 |
| `.gitignore`、`.nojekyll` | 适配新项目，忽略密钥、本地配置及临时材料；保留静态部署需要的配置 |

### 8.7 不删除及需要迁移保护的内容

- `.git/` 和已有历史。
- 新建的需求、清理、实现及运行文档。
- 新流程实际复用代码所需的许可证、第三方声明和作者归属信息；若其原目录会删除，先迁移到合适位置。
- 用户本地未跟踪的 IDE 配置、凭据和工作资料，例如 `.idea/` 不因出现在目录列表中就删除。

当前 README 有不同许可证表述，不能未经核对就替换为一种新许可证。实现时以所复用内容的实际许可为依据整理。

### 8.8 执行与验证顺序

1. 记录当前 Git 状态与基线，核实所有待操作的绝对路径位于本仓库内。
2. 提取可复用代码、来源线索和必要署名许可，形成具体文件变更。
3. 删除无关产品、DSH 与无关采集入口，构建新最小流程。
4. 新索引、页面与任务接通后，移除被替代的旧文件和依赖。
5. 验证无旧入口、无活跃 DSH 条目、无失效导航；不要求删掉需求文档中的历史说明。
6. 验证首次与每周新增及重评合计上限 50、优先复核变化的已有推荐、手动运行不绕过周额度、失败保留旧数据、候选／推荐状态和发布链路。

## 9. 验收标准

1. 仓库以新 Skills 导航为唯一产品，不残留旧助手、DSH、安装备份等运行入口。
2. 官方、社区清单、GitHub 搜索渠道均有明确配置，领域和排除规则可维护。
3. 独立技能链接准确，去重有效，不把仓库数冒充技能数。
4. 推荐区与候选区分开，推荐理由可追溯，低 Star 新技能不被机械排除。推荐技能变化后保留推荐并醒目标记待复核，复核不通过再降级，旧评估不冒充新版本验证。
5. 收录领域与第 3 节一致，金融和健康边界落实到筛选规则。
6. 已评估条目有中文简介和分类；未评估或失败状态如实展示。不按平台设置收录门槛，上游声明的特殊依赖在条目中可见。
7. 首次与后续每周新增及重评合计均不超过 50 个，优先复核变化的已有推荐；未变化条目不重复评估，手动运行不绕过额度。
8. 每周同步、手动同步、页面发布和更新报告能连通。
9. 来源失败不清空目录，索引和网页保持一致。
10. 不下载完整技能包、不执行技能、不泄露密钥。
11. 删除内容、复用代码、许可证和页面链接经过核对。

## 10. 实施建议与上线前信息

核心产品范围已通过访谈明确。以下是实施建议，不冒充用户逐项作出的选择；常规技术选型无需继续逐个提问。

### 10.1 建议采用的实现方式

- 使用 Python 实现无常驻进程的批处理流程，静态 HTML/CSS/JavaScript 展示索引，不为管理规则引入数据库服务或后台。
- 来源、领域、搜索词、排除规则、模型与额度分别配置，避免把维护规则散落在代码里。
- 人工纠错通过版本控制中的覆盖配置实现，保留修改理由；后续自动更新不能静默覆盖人工纠正。
- 筛选规则变更后，将受影响条目加入同一复核队列，计入每周 50 个总额度，明确区分内容变化与规则变化。
- 对混合用途条目，能定位为独立技能时分别处理；若单个技能将实盘下单、专业临床决策等排除功能作为不可分割的核心能力，则不因其中还包含允许功能而推荐。
- 单条读取量、输出长度、调用次数和重试次数设置上限。具体数值在模型接入时配置，不将每周 50 个技能等同于固定金额预算。
- 金融与健康类要求记录信息来源、主要限制及明显夸大风险；细化规则用样本验证，不用作者知名度或模型自评代替证据。
- 候选数据、评估状态和周额度持久化；公开页面数据由唯一索引生成。页面更新与 Actions 运行状态分别记录。

建议的新目录职责：

| 组成 | 职责 |
| --- | --- |
| `src/` | 来源发现、技能识别、预筛、模型评估、索引与报告生成 |
| `config/` | 来源、分类、搜索与排除规则、模型示例配置、人工覆盖 |
| `data/` | 唯一索引、待处理队列、评估依据、周额度与历史状态 |
| `public/` | 静态网页及从索引生成的展示数据与更新报告 |
| `tests/` | 去重、筛选、状态转换、额度、失败处理与输出验证 |
| `.github/workflows/` | 每周／手动处理和 Pages 发布 |
| `docs/` | 需求、清理清单、规则说明及部署操作说明 |

具体模块与文件名可按实现调整，不保留没有用途的空架构。

### 10.2 上线前需要补充或落实

- 目标 GitHub 仓库与发布地址：使用有权限的原仓库还是个人 fork，实施时核实。
- 模型供应商、接口、模型名称与密钥：用户尚未选择；代码先支持配置，密钥仅通过 Secrets 设置。
- 金额预算如需硬上限，应在选定供应商后配置；尚未确定金额预算，不进行无法界定费用的付费批处理。
- 每周具体运行时间由部署配置落实，首批来源和搜索规则由实施时核验并形成可审阅配置。

## 11. 实施阶段

1. **清理与骨架**：按文件清单核实引用及许可证，移除旧产品，搭建最小采集与静态展示结构。
2. **采集与规则**：核实官方和社区来源，实现 GitHub 搜索、去重、领域过滤、DSH 等排除与配置维护。
3. **AI 与索引**：接入可配置模型接口、中文简介、分类、候选／推荐、版本依据、人工覆盖及额度队列。
4. **网页与报告**：实现搜索筛选、更新历史、特殊依赖展示和待复核提示，保证索引与网页一致。
5. **验证与发布**：用测试材料验证流程，配置密钥后按首次最多 50 个处理并检查实际结果，再启用每周运行和 Pages 发布。

本轮仅合并整理文档，尚未执行本文计划的旧项目清理、程序改造、模型调用或部署。
