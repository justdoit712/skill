# 运行依赖基线（依据 docs/Skills导航目录二开需求.md §7.4）
#
# JSON、时间与静态模板生成使用标准库，不引入额外依赖。
# 已按 §7.4 移除旧助手、常驻服务与本地调度专用依赖：
#   schedule、flask、flask-cors（常驻调度与 API 服务）
#   beautifulsoup4、lxml（旧 HTML 爬取）
#
# 开发与测试依赖单独管理，不混入本文件。
# 版本号在选定环境通过干净安装与检查后锁定，不把建议版本当成已验证版本。

requests>=2.31.0
PyYAML>=6.0
