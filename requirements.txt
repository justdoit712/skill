# 运行依赖基线（依据 docs/产品规范.md §7.4）
#
# JSON、时间与静态模板生成使用标准库，不引入额外依赖。
# HTTP 请求使用 requests，元数据解析使用 PyYAML。
# 项目采用批处理与静态页面，不需要常驻 API 服务或本地调度框架。
#
# 开发与测试依赖单独管理，不混入本文件。
# 版本号在选定环境通过干净安装与检查后锁定，不把建议版本当成已验证版本。

requests>=2.31.0
PyYAML>=6.0
